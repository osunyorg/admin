<% content_for :title, 'Potentiel de contribution' %>

<div class="row">
  <% @universities.each do |university| %>
    <div class="col-lg-3">
      <article class="card">
        <div class="card-body">
          <%= link_to university, [:server, university], class: 'stretched-link' %>
        </div>
      </article>
    </div>
  <% end %>
</div>