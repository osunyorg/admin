var notyf = new Notyf();
notyf.open({
    type: 'success',
    position: {
        x: 'center',
        y: 'bottom'
    },
    message: "<%= j(t('server_admin.websites.update_theme_notice', website: @website.to_s)) %>",
    duration: 5000,
    ripple: true,
    dismissible: true
});
