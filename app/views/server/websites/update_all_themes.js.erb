var notyf = new Notyf();
notyf.open({
    type: 'success',
    position: {
        x: 'center',
        y: 'bottom'
    },
    message: "<%= j(t('server_admin.websites.update_all_themes_notice')) %>",
    duration: 5000,
    ripple: true,
    dismissible: true
});
