<div class="row g-3">
  <% organizations.each do |organization| %>
    <div class="col-6 col-lg-3 col-xxl-2">
      <%= render 'admin/university/organizations/organization', organization: organization %>
    </div>
  <% end %>
</div>