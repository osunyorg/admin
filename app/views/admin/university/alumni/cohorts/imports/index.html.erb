<% content_for :title, t('university.alumni.cohorts.title') %>

<%= render 'filters', current_path: admin_university_alumni_cohorts_imports_path, filters: @filters if @filters.any?  %>

<%= render 'admin/imports/list', imports: @imports, path_pattern: 'admin_university_alumni_cohorts_import_path' %>

<% content_for :action_bar_right do %>
  <%= link_to_if  can?(:create, University::Person::Alumnus),
                  t('create'),
                  new_admin_university_alumni_cohorts_import_path,
                  class: button_classes %>
<% end %>
