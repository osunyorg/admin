<%
# Ca devrait être une vraie liste, pas une grille, sinon c'est confusant.
# Il faudrait l'utiliser dans les formations.
%>
<div class="row g-3">
  <% teachers.each do |person| %>
    <div class="col-6 col-lg-3 col-xxl-2">
      <div class="<%= osuny_card_classes %>">
        <%= osuny_thumbnail_localized person, large: true %>
        <div class="card-body">
          <p class="mb-1">
            <%= osuny_published_localized person %>
            <%= osuny_link_localized  person,
                                      admin_education_teacher_path(person),
                                      classes: 'stretched-link'  %>
          </p>
        </div>
      </div>
    </div>
  <% end %>
</div>