<%  [
    :presentation,
    :objectives
    ].each do |property| %>
    <%= @program.send(property).to_s %>
<% end %>

<%= render 'admin/communication/blocks/preview', about: @program %>

<%  [
    :opportunities,
    :results,
    :accessibility,
    :duration,
    :pricing,
    :content,
    :pedagogy,
    :evaluation,
    :prerequisites,
    :registration,
    :other,
    :contacts
    ].each do |property| %>
    <%= @program.send(property).to_s %>
<% end %>
