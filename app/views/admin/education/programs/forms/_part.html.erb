<div class="card">
  <div class="card-header">
    <h5 class="card-title mb-0">
      <%= t("education.program.parts.#{part}.label") %>
    </h5>
  </div>
  <div class="card-body">
    <p><%= t("education.program.parts.#{part}.hint") %></p>
    <%= yield %>
  </div>
</div>
