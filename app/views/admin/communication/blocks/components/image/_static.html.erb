<%
component = template.public_send "#{property}_component"
blob = component.blob
indentation = '      '
%>
<% if blob %>
<%= indentation %>image:
<%= indentation %>  id: "<%= blob.id %>"
<%= indentation %>  file: "<%= blob.id %>"
<% end %>
