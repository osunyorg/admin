<%
blob = component.blob
list ||= false
return unless blob
%>
<% if list %>
<%= indentation %>- id: "<%= blob.id %>"
<%= indentation %>  file: "<%= blob.id %>"
<% else %>
<%= indentation %><%= property %>:
<%= indentation %>  id: "<%= blob.id %>"
<%= indentation %>  file: "<%= blob.id %>"
<% end %>
