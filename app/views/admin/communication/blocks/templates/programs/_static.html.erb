      layout: <%= block.template.layout %>
<%= block_options_static block %>
      programs:
<% block.template.selected_programs.each do |program| 
    l10n = program.localization_for(block.language)
    next if l10n.nil? %>
        - path: "<%= l10n.path %>"
<% end %>