      images:
<% if block.data.has_key?('elements') %>
<% block.data['elements'].each do |image| %>
        - alt: >
            <%= prepare_text_for_static image['alt'] %>
          file: "<%= prepare_media_for_static image, 'file' %>"
<% end %>
<% end %>
