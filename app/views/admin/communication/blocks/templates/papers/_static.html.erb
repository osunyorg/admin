      papers:
<% 
block.template.selected_papers.each do |paper| 
  paper_l10n = paper.localization_for(block.language)
  next if paper_l10n.nil?
  hugo = paper_l10n.hugo(@website)
  %>
        - permalink: "<%= hugo.permalink %>"
          file: "<%= hugo.file %>"
          path: "<%= hugo.path %>"
          slug: "<%= hugo.slug %>"
<% end %>
