      papers:
<% block.template.selected_papers.each do |element| %>
<% byebug %>
        - path: "<%= element.path %>"
          slug: "<%= element.slug %>"
          file: "<%= element.git_path_relative %>"
<% end %>
