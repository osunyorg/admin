      volumes:
<%
block.template.selected_volumes.each do |volume|
  volume_l10n = volume.localization_for(block.language)
  next if volume_l10n.nil?
  hugo = volume_l10n.hugo(@website)
  %>
        - "<%= volume_l10n.relative_path %>"
<% end %>
