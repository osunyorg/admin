<%
hide_date ||= false
hide_size ||= false
if file.deleted?
  partial_name = 'deleted'
else
  partial_name = 'default'
end
%>
<%= render "admin/communication/library/files/file/#{partial_name}",
            file: file,
            hide_date: hide_date,
            hide_size: hide_size %>
