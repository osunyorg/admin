<div class="row g-2 mb-3">
  <% categories.each do |category| %>
    <div>
      <div class="card card--horizontal">
        <%= osuny_thumbnail_localized category %>
        <div class="card-body">
          <%= osuny_published_localized category %>
          <%= osuny_link_localized  category, 
                                    admin_communication_extranet_document_category_path(extranet_id: category.extranet.id, id: category.id),
                                    classes: 'stretched-link' %>
        </div>
        <div class="card-footer">
          <%= category.documents.count %>
          <%= Communication::Extranet::Document.model_name.human(count: category.documents.count).downcase %>
        </div>
      </div>
    </div>
  <% end %>
</div>
