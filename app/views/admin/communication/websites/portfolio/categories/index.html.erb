<% content_for :title, "#{Communication::Website::Portfolio::Category.model_name.human(count: 2)} (#{@categories.count})" %>

<%= render 'admin/application/categories/panel', 
            root_categories: @root_categories, 
            categories_class: @categories_class %>

<%= render 'admin/communication/websites/language_switcher' %>
