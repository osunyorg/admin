<%
if @permalink.valid?
  html = escape_javascript(render 'admin/communication/websites/permalinks/permalink', permalink: @permalink)
  %>
  var tbody = document.querySelector('#previous-permalinks tbody');
  tbody.insertAdjacentHTML("beforeend", "<%= html %>");
<%
else
  message = @permalink.errors.full_messages.to_sentence
  %>
  notyf = new Notyf();
  notyf.open({
      type: 'error',
      position: {
          x: 'left',
          y: 'bottom'
      },
      message: "<%= message %>",
      duration: 9000,
      ripple: true,
      dismissible: true
  });
  <%
end
%>
