<%# TODO: Title should be removed safely as website title is now in languages config file. %>
<%= render 'admin/application/static/title', about: @website.original_localization.to_s %>
default:
<% if @website.default_image.attached? %>
  image:
    id: <%= @website.default_image.blob&.id %>
<% end %>
<% if @website.default_shared_image.attached? %>
  shared_image:
    id: <%= @website.default_shared_image.blob&.id %>
<% end %>