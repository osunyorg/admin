<%
month = @l10n.about
language = @l10n.language
%>---

<%= render 'admin/application/static/core',
            about: @l10n,
            skip_date: true,
            forced_title: @l10n.to_month_and_year %>
<%= render 'admin/application/static/breadcrumbs', about: @l10n %>
<%= render 'admin/application/static/design',
            full_width: true,
            toc_present: false,
            toc_offcanvas: false %>
<%= render 'admin/application/l10n/static', about: @l10n %>
days:
<%
month.days.each do |day| 
  events = month.events_for(day)
  next if events.none?
  %>
  - day: <%= day.iso8601 %>
    events:
      count: <%= events.count %>
<%= render 'admin/application/static/hugo/collection',
            key: :list,
            collection: events,
            language: language,
            depth: 3 %>
<% end %>
---