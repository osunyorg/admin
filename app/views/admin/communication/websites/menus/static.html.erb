<%
title = @about.title
items = @about.items
              .root
              .ordered
              .map(&:to_static_hash)
              .compact
hash = { 
  'title' => title,
  'items' => items
}
%>
<%= raw hash.to_yaml %>
