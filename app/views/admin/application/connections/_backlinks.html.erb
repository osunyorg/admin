backlinks:
  current_website:
    name: "<%= @website %>"
    posts:
<% about.backlinks_posts(@website).each do |element| %>
      - path: "<%= element.static_path %>"
        file: "<%= element.git_path_relative %>"
<% end %>
