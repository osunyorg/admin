backlinks:
  current_website:
    name: "<%= @website %>"
    pages:
<% object.backlinks_pages(@website).each do |element| %>
      - path: "<%= element.path %>"
        file: "<%= element.git_path_relative %>"
<% end %>
    posts:
<% object.backlinks_posts(@website).each do |element| %>
      - path: "<%= element.static_path %>"
        file: "<%= element.git_path_relative %>"
<% end %>
    events:
<% object.backlinks_agenda_events(@website).each do |element| %>
      - path: "<%= element.current_permalink_in_website(@website)&.path %>"
        file: "<%= element.git_path_relative %>"
<% end %>
    projects:
<% object.backlinks_portfolio_projects(@website).each do |element| %>
      - path: "<%= element.current_permalink_in_website(@website)&.path %>"
        file: "<%= element.git_path_relative %>"
<% end %>
